[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "meta-search-rsi"
version = "0.1.0"
description = "Read-only heterogeneous memory and prefix-only historical replay"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

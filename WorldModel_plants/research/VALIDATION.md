# 実装の検証記録

実施日: 2026-09-15 JST。

## 環境

- Mac / arm64、macOS 26.5、Python 3.12.14。
- JAX / jaxlib 0.11.1、NumPy 2.5.3、matplotlib 3.11.2、Pillow 12.3.0。
- Codex CLI 0.154.0-alpha.6.1。モデル指定と取得ログ: `gpt-6-astra` / `low`。
- 実験の参照元commit: `b5c471dda8f12d3c2451d2da642521db178d988c`。
  未コミットのSimulator高速化なども含め、実際に使う参照ファイルをrun内へコピーし、
  hashを保存する。commitだけでは今回の実行状態を再現できない。

## 自動チェック

既存`tests/`の186件と、追加した`research/tests/`の21件が通過した。
既存テストとの一括実行は追加テスト17件の時点で203件を確認し、
追加の復帰・ログ解析テストを含む最新版21件を改めて実行した。

```bash
JAX_PLATFORMS=cpu .venv/bin/python -m pytest research/tests tests -q
```

以下を検証している。

- 操作を与えた後に出力が記録される時刻対応と、予測起点以降の観測を入力しないこと。
- 30/300/900ステップのRMSE集計、固定尺度、元の制御costとの数式の対応。
- 元のランダム操作生成との一致、許可した操作範囲、物理設定の変更拒否。
- 許可外ファイル変更・削除・symlinkの拒否。
- timeout後に次の処理を起動できること。失敗しても最良候補と追加データを保持すること。
- token重複排除、累積値との二重加算防止、不完全JSONL、usage欠損の区別。
- 応答IDとtoolの推定対応の明示、採否を含むタイムライン、評価失敗前の計算量の保存。
- 共通ラウンドまでの候補選択、無効な数値を最良候補にしないこと、モデル設定の固定。

`git diff --check`とPython構文チェックも通過した。

## 短縮した統合実行

Run: `20260915T045134Z_09b058`。約223秒で完了。
初期モデル→A/B各1回の追加データ生成→学習→検証→未使用データの生成→最終評価→
グラフと3本のGIF生成を確認した。

この実行はCodexを呼ばず、学習時間・MPCの計算量・制御評価長を短縮している。
数値は動作確認のためのもので、研究上の性能改善の根拠には使用しない。

後処理の拡張後にも保存済みの短縮runからレポートを再生成し、
予測72行、制御12行、実験資源3行のCSVを確認した。

## 本実験との分離

本実験では、開始時に凍結した`reference/`のコードで生成・学習・採点する。
実行中に追加したCSV出力とtool対応の解析は、終了後に保存済みログへ適用する
後処理であり、候補・評価値・採用基準には反映しない。
後処理コードのhashは`analysis.json`に記録する。

本実験`20260915T045740Z_d0631e`は、8候補と3モデルの最終評価を完了し、
自動レポートまで3820秒だった。3本のGIFは各100フレーム・900×540。
予測144行・制御24行・応答48行のCSVと、両制御シナリオのグラフを確認した。
参照hash、編集範囲、学習入力、固定尺度、1600ステップ・目標変更300、
候補選択後のテスト生成、seed分離、CLIと応答別token合計の一致を監査した。
結果の解釈と監査ファイルへのリンクは[RESULTS_20260915.md](RESULTS_20260915.md)にまとめた。

単一のA/B比較であり、再現性・統計的有意性・スケーリング則の検証は別工程。
